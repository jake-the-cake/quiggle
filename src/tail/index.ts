import * as RequestHandlers from './express-controllers/handleRequest'

const {
  initResponseObject,
  setDataResponse
} = RequestHandlers

export {
  RequestHandlers,
  initResponseObject,
  setDataResponse
}
